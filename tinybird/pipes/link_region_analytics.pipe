
NODE region_node
DESCRIPTION >
    Get link click analytics by region/state for a specific link.

SQL >
    %
    SELECT
        location_region as region,
        count() as total_clicks
    FROM link_clicks
    WHERE
        profileUserId = {{String(profileUserId, required=True)}} AND
        linkId = {{String(linkId, required=True)}} AND
        location_region != ''
    GROUP BY region
    ORDER BY total_clicks DESC

TYPE endpoint